import React from 'react';
import './Cart.css';

const Cart = () => {
    return (<div className="Cart" onClick={() => null}>
        <p>Cart</p>
        <img src={''}></img>
    </div>);
}

export default Cart;